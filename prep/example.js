let greet = "Hello";
Math.round(13.4);
let GreetExample = `${greet} there, example`;
console.log("hello there!");
function convertToPercentage(decimal) {
    return `${decimal * 100}%`
}

const height = 10;
const width = 10; 

const area = height * width;
const perimeter = (height + height) + (width + width);
console.log(area);
console.log(perimeter);

function areaofRectangle(high, wide) {
    return high * wide; 
}

function perimeterofRectangle(height, width) {
    return height + height + width + width;
}